description = 'OpenCensus Logging Exporter'

dependencies {
    compile project(':opencensus-api'),
            libraries.guava

    testCompile project(':opencensus-api')

    signature "org.codehaus.mojo.signature:java16:+@signature"
}