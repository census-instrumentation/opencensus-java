description = 'Instrumentation Core Impl'

dependencies {
    compile project(':instrumentation-java-core'),
            project(':proto'),
            libraries.disruptor,
            libraries.guava

    testCompile project(':instrumentation-core')
}
